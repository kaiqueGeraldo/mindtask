import AprovarVinculoScreen from "@/screens/(aprovar-vinculo)/aprovar-vinculo";

export const metadata = {
  title: "MindTask - Aprovar Vinculo",
};

export default function AprovarPage() {
  return <AprovarVinculoScreen />;
}
