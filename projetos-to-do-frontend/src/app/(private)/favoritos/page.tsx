import FavoritosScreen from "@/screens/(favoritos)/favoritosScreen";

export const metadata = {
  title: "MindTask - Favoritos",
};

export default function FavoritosPage() {
  return <FavoritosScreen />;
}
