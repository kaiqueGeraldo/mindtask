import SettingsScreen from "@/screens/(configuracoes)/settingsScreen";

export const metadata = {
  title: "MindTask - Confugurações",
};

export default function SettingsPage() {
  return <SettingsScreen />;
}
